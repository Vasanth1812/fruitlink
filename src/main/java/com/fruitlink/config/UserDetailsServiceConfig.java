package com.fruitlink.config;

// This stub is no longer needed — the real UserDetailsService bean is
// provided by com.fruitlink.auth.service.UserDetailsServiceImpl (@Service).
// This file can be safely deleted.
//
// Keeping it here as an empty placeholder so nothing breaks during refactor.
public class UserDetailsServiceConfig {
    // intentionally empty
}
